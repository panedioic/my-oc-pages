<% if (page.total > 1) { %>
    <nav class="pagination">
        <%- paginator({
            prev_text: '上一页',
            next_text: '下一页',
            mid_size: 1,
            end_size: 1
        }) %>
    </nav>
<% } %> 